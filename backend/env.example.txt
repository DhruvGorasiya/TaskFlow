DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/taskflow

CANVAS_API_URL=https://yourcanvas.example.com
CANVAS_API_TOKEN=your_canvas_token_here

# Notion (push tasks to a Notion database). Create a DB with properties:
# Name (title), Description (rich_text), Due Date (date), Priority (select),
# Status (select), Course (rich_text), Source (rich_text).
# Select options: Priority = high|medium|low|none, Status = pending|completed|archived.
NOTION_API_TOKEN=your_notion_integration_token
NOTION_DATABASE_ID=your_notion_database_id

# Copy this file to `env.local.txt` (DO NOT COMMIT) for local development.

# Optional: scheduler (disabled by default)
ENABLE_SCHEDULER=false
SYNC_INTERVAL_MINUTES=15
